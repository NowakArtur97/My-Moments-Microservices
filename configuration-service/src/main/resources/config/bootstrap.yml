#
# Spring
#
spring:
  application:
    name: configuration-service
  profiles:
    active: ${SPRING_ACTIVE_PROFILE:dev}
#    active: vault, git, local # unlock Vault and Git configs
#
# Server
#
server:
  port: 8888