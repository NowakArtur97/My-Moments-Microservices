#
# Spring
#
spring:
  application:
    name: gateway-service
  profiles:
    active: ${SPRING_ACTIVE_PROFILE:dev}
#
# Server
#
server:
  port: 8989
#
# Logging
#
logging:
  level:
    org:
      springframework:
        cloud:
          gateway: TRACE