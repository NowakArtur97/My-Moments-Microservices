import { PasswordRulesDirective } from './password-rules.directive';

describe('PasswordRulesDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordRulesDirective();
    expect(directive).toBeTruthy();
  });
});
