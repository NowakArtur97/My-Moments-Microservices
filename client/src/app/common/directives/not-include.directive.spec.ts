import { NotIncludeDirective } from './not-include.directive';

describe('NotIncludeDirective', () => {
  it('should create an instance', () => {
    const directive = new NotIncludeDirective();
    expect(directive).toBeTruthy();
  });
});
