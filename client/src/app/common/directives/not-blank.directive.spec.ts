import { NotBlankDirective } from './not-blank.directive';

describe('NotBlankDirective', () => {
  it('should create an instance', () => {
    const directive = new NotBlankDirective();
    expect(directive).toBeTruthy();
  });
});
