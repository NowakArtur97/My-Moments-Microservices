#
# Spring
#
spring:
  application:
    name: post-service
  profiles:
    active: ${SPRING_ACTIVE_PROFILE:dev}
#
# Server
#
server:
  port: 8082
#
# Custom properties
#
my-moments:
  comment-service-uri: lb://comment-service/api/v1/posts/{postId}/comments